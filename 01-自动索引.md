# 📇 卡片自动索引（Dataview 实时生成）

> 本页由 **Dataview** 插件自动扫描 `categories/` 下所有卡片生成，**无需手动维护**。
> 在 Obsidian 中打开即实时更新；元数据（分类/关联度/变现）仍以 `index.md` 为准。

## 全部卡片（按文件名倒序 = 最新在上）
```dataview
TABLE file.folder AS 所在分类, file.mtime AS 最后更新
FROM "categories"
SORT file.name DESC
```

## 按一级分类拆看
### AI与方法
```dataview
LIST FROM "categories/AI与方法" SORT file.name DESC
```
### 创业与变现
```dataview
LIST FROM "categories/创业与变现" SORT file.name DESC
```
### 技术与工具
```dataview
LIST FROM "categories/技术与工具" SORT file.name DESC
```
### 行业与趋势
```dataview
LIST FROM "categories/行业与趋势" SORT file.name DESC
```
### 个人成长
```dataview
LIST FROM "categories/个人成长" SORT file.name DESC
```

## 空卡 / 待补检测
```dataview
LIST FROM "categories"
WHERE length(file.content) < 200
```
